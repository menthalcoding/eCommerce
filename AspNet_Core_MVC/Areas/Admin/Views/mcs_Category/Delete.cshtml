@model eCommerce.Models.mcs_Category

@{
	ViewData["Title"] = "Delete - mcs_Category";
	Layout="~/Pages/Shared/_Layout.cshtml";
}

@using (Html.BeginForm())
{
    <h1>mcs_Category - Delete</h1>
    <h3>Are you sure you want to delete this?</h3>

        <div class="display-label">CategoryId</div>
        <div class="display-field">@Html.DisplayFor(m => m.CategoryId)</div>
        <div class="display-label">CategoryParentId</div>
        <div class="display-field">@Html.DisplayFor(m => m.CategoryParentId)</div>
        <div class="display-label">Name</div>
        <div class="display-field">@Html.DisplayFor(m => m.Name)</div>
        <div class="display-label">Description</div>
        <div class="display-field">@Html.DisplayFor(m => m.Description)</div>
    <p>
        <input type="submit" value="Delete" />
    </p>

    <p>
    @Html.ActionLink("Back to List", "Index")
    </p>
}